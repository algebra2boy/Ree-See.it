import express from 'express'

const router = express.Router()

router.post('/api/receipt/:user_id', (req, res) => {

})

router.get('/api/receipt/:user_id', (req, res) => {
  
})

export default router